## Purpose
<!-- Только для нового воркфлоу: 1–2 фразы (от 50 символов), какую бизнес-задачу решает. Для существующего — удалить раздел. -->

## ADDED Requirements

### Requirement: Trigger
<!-- Что запускает воркфлоу: вебхук / расписание / событие / вызов из другого воркфлоу. Частота, ожидаемая нагрузка. -->
The workflow SHALL ...

#### Scenario: <!-- имя сценария -->
- **WHEN** <!-- условие -->
- **THEN** <!-- наблюдаемый результат -->

### Requirement: Input contract
<!-- Структура входа словами. Нетривиальный контракт — ссылка на tests/<имя>/input.schema.json. Что делать с невалидным входом. -->
The workflow SHALL ...

#### Scenario: Валидный вход
- **WHEN** ...
- **THEN** ...

#### Scenario: Невалидный вход
- **WHEN** ...
- **THEN** ...

### Requirement: Output and side effects
<!-- Что возвращает, куда пишет, кого уведомляет. -->
The workflow SHALL ...

#### Scenario: <!-- имя сценария -->
- **WHEN** ...
- **THEN** ...

### Requirement: Error handling
<!-- Поведение при недоступности каждой внешней системы: повторы, таймауты, куда попадает ошибка. -->
The workflow SHALL ...

#### Scenario: Внешняя система недоступна
- **WHEN** ...
- **THEN** ...

### Requirement: Idempotency
<!-- Что происходит при повторном запуске с теми же данными. Если невозможно — сказать явно и почему. -->
The workflow SHALL ...

#### Scenario: Повторный запуск с теми же данными
- **WHEN** ...
- **THEN** ...

### Requirement: Credentials
<!-- Внешние системы и имена credentials в n8n. Значения секретов запрещены. -->
The workflow MUST use only the following named credentials: ...

#### Scenario: Credential отсутствует в инстансе
- **WHEN** ...
- **THEN** ...

<!-- Далее — бизнес-требования воркфлоу, каждое со своими сценариями. -->

### Requirement: <!-- бизнес-требование -->
The workflow SHALL ...

#### Scenario: <!-- имя сценария -->
- **WHEN** ...
- **THEN** ...
