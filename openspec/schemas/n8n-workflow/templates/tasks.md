## 1. Тест-кейсы

- [ ] 1.1 Создать tests/<имя>/case-01.input.json и case-01.expected.json для сценария «<имя сценария>» — проверка: файлы валидный JSON, покрыт каждый Scenario спеки

## 2. Сборка воркфлоу в n8n (выключенным)

- [ ] 2.1 Собрать воркфлоу «<имя>» по Node map из design.md через n8n MCP / REST API — проверка: воркфлоу открывается в n8n, узлы соответствуют Node map
- [ ] 2.2 Добавить Sticky Note со ссылкой на спеку и назначить Error Workflow — проверка: видно в редакторе и в settings.errorWorkflow

## 3. Прогон

- [ ] 3.1 Прогнать case-*.input.json через тестовый воркфлоу или pin data — проверка: фактический выход совпадает с case-*.expected.json

## 4. Проверка и фиксация

- [ ] 4.1 Все тест-кейсы tests/<имя>/ проходят — проверка: результат прогона совпадает с case-*.expected.json
- [ ] 4.2 Экспорт и нормализация — проверка: `npm run export -- <имя>` обновил workflows/<имя>.json, `git diff` показывает только ожидаемые изменения
- [ ] 4.3 Конституция соблюдена — проверка: Sticky Note со ссылкой на спеку, Error Workflow назначен, Code-ноды ≤ 50 строк, в JSON нет секретов (`npm run check -- <имя>`)
- [ ] 4.4 Спека и JSON закоммичены вместе — проверка: один коммит содержит openspec/changes/<change>/ и workflows/<имя>.json
- [ ] 4.5 Воркфлоу активирован с согласия пользователя — проверка: пользователь подтвердил, в n8n active=true
