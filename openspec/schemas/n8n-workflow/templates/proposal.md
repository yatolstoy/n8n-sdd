## Why

<!-- 1–2 фразы: какая бизнес-проблема и почему сейчас. -->

## What Changes

<!-- Список изменений в поведении воркфлоу. Ломающие изменения контракта входа/выхода — **BREAKING**. -->

## Capabilities

### New Capabilities
<!-- Новые воркфлоу. Имя capability = имя воркфлоу в n8n = workflows/<имя>.json (kebab-case).
     Каждый создаёт specs/<имя>/spec.md. -->
- `<имя-воркфлоу>`: <что делает воркфлоу, одна фраза>

### Modified Capabilities
<!-- Существующие воркфлоу, у которых меняются ТРЕБОВАНИЯ (не реализация).
     Точный путь из openspec/specs/. Пусто, если требования не меняются.
     Изменение без capabilities (косметика, инструменты, документация) — skip_specs: true в .openspec.yaml. -->
- `<существующий-воркфлоу>`: <какое требование меняется>

## Impact

<!-- Внешние системы, имена credentials (без значений), воркфлоу, которые вызывают этот или вызываются им. -->
