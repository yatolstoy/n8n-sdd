## Context

<!-- Текущее состояние воркфлоу в n8n и ограничения. Мотивация — в proposal.md, не повторять. -->

## Node map

<!-- Первым узлом — Sticky Note с текстом «Spec: openspec/specs/<имя>/spec.md».
     Далее: имя ноды → тип ноды → какое требование спеки реализует. Ветвления — словами или mermaid. -->

| # | Нода | Тип | Требование спеки |
|---|------|-----|------------------|
| 0 | Spec note | Sticky Note | — |
| 1 | | | |

## Credentials

<!-- Только имена credentials, без значений. -->

| Внешняя система | Имя credential в n8n | Нужные права |
|-----------------|----------------------|--------------|
| | | |

## Code nodes

<!-- Имя → что делает → оценка размера. Больше 50 строк запрещено — указать, куда выносим. Если Code-нод нет — «нет». -->

## Error Workflow

<!-- Какой Error Workflow назначен (settings.errorWorkflow), куда идут уведомления, что логируется. -->

## Environment

<!-- Инстанс один и рабочий. Переменные окружения, webhook-пути, расписания. Какие внешние записи делают тесты и как их избежать (тестовые записи, pin data). -->

## Decisions

<!-- Ключевые решения: почему X, а не Y. Рассмотренные альтернативы. -->

## Risks / Trade-offs

<!-- [Риск] → как смягчаем. -->

## Open Questions

<!-- Только то, что можно решить позже без изменения спеки и задач. Если нет — удалить раздел. -->
